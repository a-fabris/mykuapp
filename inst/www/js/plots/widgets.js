/*
$(function() {
  $( "#histo-binwidth" ).slider({
      value: HISTOGRAM_BINS,
      min: 1,
      max: 50,
      step: 1,
      slide: function( event, ui ) {
        $( "#amount" ).val( "$" + ui.value );
      }
    });
    
    $( "#amount" ).val( "$" + $( "#histo-binwidth" ).slider( "value" ) );
});

*/